# Server-only secrets (do not prefix with NEXT_PUBLIC_)
GEMINI_API_KEY="your-gemini-api-key"
GOOGLE_CALENDAR_ID="your-calendar-id@example.com"
GOOGLE_SERVICE_ACCOUNT_KEY_PATH="./credentials/keys.json"
# Optional: if storing raw JSON credentials instead of a key file path
# GOOGLE_SERVICE_ACCOUNT_KEY_JSON='{"type":"service_account",...}'
